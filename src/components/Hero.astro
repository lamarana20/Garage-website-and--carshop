<section class="relative min-h-[70vh] flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-950 via-slate-900 to-gray-900">
  <!-- Static background decoration - removed animations -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-red-500 rounded-full filter blur-3xl"></div>
    <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-gray-500 rounded-full filter blur-3xl"></div>
  </div>
  
  <!-- Grid pattern overlay -->
  <div class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,.02)_1px,transparent_1px)] bg-[size:50px_50px]"></div>
  
  <!-- Content -->
  <div class="relative z-10 w-full max-w-6xl mx-auto px-4 sm:px-6 text-center py-20 sm:py-0">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-3 py-1.5 sm:px-4 sm:py-2 mb-6 sm:mb-8 text-xs sm:text-sm">
      <span class="w-2 h-2 bg-green-400 rounded-full"></span>
      <span class="text-gray-200">Open Now • Fast Service</span>
    </div>
    
    <!-- Main heading -->
    <h1 class="text-2xl sm:text-4xl md:text-5xl font-black mb-2 sm:mb-4 leading-snug px-2">
      <span class="bg-gradient-to-r from-white via-gray-100 to-red-400 bg-clip-text text-transparent">
        Premium Auto Care
      </span>
      <br />
      <span class="text-white">Built for Performance</span>
    </h1>
    
    <!-- Description -->
    <p class="text-sm sm:text-lg md:text-xl text-gray-300 mb-6 sm:mb-8 max-w-2xl mx-auto leading-relaxed px-4">
      Expert mechanics, cutting-edge diagnostics, and quality vehicles. 
      <span class="text-red-400 font-semibold">Your car deserves the best.</span>
    </p>
    
    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center mb-10 sm:mb-12 px-4">
      <a href="/#contact" 
         class="group relative w-full sm:w-auto px-6 sm:px-8 py-3 sm:py-4 bg-red-600 text-white font-bold rounded-lg transition-all hover:bg-red-700 text-sm sm:text-base">
        <span class="relative z-10">Schedule Service</span>
      </a>
      
      <a href="/Inventory" 
         class="group w-full sm:w-auto px-6 sm:px-8 py-3 sm:py-4 bg-white/10 backdrop-blur-sm text-white font-bold rounded-lg border-2 border-white/20 transition-all hover:bg-white/20 text-sm sm:text-base">
        Browse Vehicles
        <span class="inline-block ml-2 transition-transform group-hover:translate-x-1">→</span>
      </a>
    </div>
    
    <!-- Stats -->
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 sm:gap-6 max-w-4xl mx-auto px-4">
      <div class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-lg sm:rounded-xl p-4 sm:p-6 transition-colors hover:bg-white/10">
        <div class="text-2xl sm:text-3xl font-bold text-red-400 mb-1 sm:mb-2">15+</div>
        <div class="text-gray-400 text-xs sm:text-sm">Years Experience</div>
      </div>
      <div class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-lg sm:rounded-xl p-4 sm:p-6 transition-colors hover:bg-white/10">
        <div class="text-2xl sm:text-3xl font-bold text-red-400 mb-1 sm:mb-2">5000+</div>
        <div class="text-gray-400 text-xs sm:text-sm">Cars Serviced</div>
      </div>
      <div class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-lg sm:rounded-xl p-4 sm:p-6 transition-colors hover:bg-white/10">
        <div class="text-2xl sm:text-3xl font-bold text-red-400 mb-1 sm:mb-2">98%</div>
        <div class="text-gray-400 text-xs sm:text-sm">Satisfaction Rate</div>
      </div>
      <div class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-lg sm:rounded-xl p-4 sm:p-6 transition-colors hover:bg-white/10">
        <div class="text-2xl sm:text-3xl font-bold text-red-400 mb-1 sm:mb-2">24/7</div>
        <div class="text-gray-400 text-xs sm:text-sm">Emergency Service</div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <a href="#about" class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce" aria-label="Scroll to content">
    <div class="w-5 h-8 sm:w-6 sm:h-10 border-2 border-white/30 rounded-full flex justify-center">
      <div class="w-1 h-2 sm:h-3 bg-white rounded-full mt-1.5 sm:mt-2"></div>
    </div>
  </a>
</section>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* Optimize animations */
  @media (prefers-reduced-motion: no-preference) {
    .animate-bounce {
      animation: bounce 2s infinite;
    }
  }
  
  @media (prefers-reduced-motion: reduce) {
    .animate-bounce {
      animation: none;
    }
  }
</style>